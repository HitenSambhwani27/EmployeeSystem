@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model IEnumerable<December_Project.Models.Employee>
@{
    ViewData["Title"] = "DisplayEmployee";

}
<table class="table table-bordered">
    <thead>
        <tr>
            <th>Photo</th>
            <th>Firstname</th>
            <th>Lastname</th>
            
            <th>Send Request</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var data in Model)
        {
                    <tr>
                        <td>
                        <img src="~/images/@data.PhotoUrl" width="50">

                    </td>
                    <td>@data.employeefirstname</td>
                    <td>@data.employeelastname</td>
                    
                    <td>
                        <a href="/Home/AddValue?PhotoUrl=@data.PhotoUrl" class="btn btn-danger delete" onclick="return confirm('Are you sure ')" title="Accept"> </a>
                        
                        
                       @* <a href="/Home/AddValue?PhotoUrl=@data.PhotoUrl" >
                            <button type="submit" style="background-color:darkgoldenrod" onclick="return" > Send Request </button>
                            <i class="fa-solid fa-trash"></i>

                        </a>*@
            @*        <a href="@Url.Action("Bdgequeue")" class="btn btn-danger delete" onclick="return confirm('Are you sure ')" title="delete">
                        <i class="fa-solid fa-trash"></i>

                    </a>*@

                     </td>

                    </tr>
        }
    </tbody>
</table>
